{
  "name": "linear-opencode-agent",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Self-hosted Linear agent powered by OpenCode",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "@linear/sdk": "^73.0.0",
      "@opencode-ai/sdk": "1.2.15",
      "@opencode-ai/plugin": "1.2.15",
      "better-result": "^2.7.0",
      "zod": "^4.3.6"
    }
  },
  "scripts": {
    "prepare": "lefthook install || true",
    "start": "bun run --filter @linear-opencode-agent/server start",
    "dev": "bun run --filter @linear-opencode-agent/server dev",
    "typecheck": "tsc --noEmit",
    "lint:check": "oxlint --report-unused-disable-directives --deny-warnings --type-aware --type-check",
    "lint:fix": "oxlint --fix",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write .",
    "knip": "knip",
    "check": "bun run typecheck && bun run lint:check && bun run format:check && bun run knip && bun test",
    "fix": "bun run lint:fix && bun run format:fix"
  },
  "devDependencies": {
    "bun-types": "^1.3.8",
    "knip": "^5.83.0",
    "lefthook": "^2.1.0",
    "oxlint": "^1.43.0",
    "oxlint-tsgolint": "^0.11.4",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  },
  "author": "",
  "license": "MIT"
}
