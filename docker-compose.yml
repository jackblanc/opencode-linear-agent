services:
  # Linear webhook server - handles OAuth and webhooks from Linear
  linear-webhook:
    build:
      context: .
      dockerfile: packages/local/Dockerfile
    ports:
      - "3000:3000" # Local access only
    volumes:
      # Mount config file (use config.docker.json for Docker networking)
      - ./config.docker.json:/app/packages/local/config.json:ro
      # Persist data (tokens, sessions)
      - linear-data:/data
      # Mount all repositories - paths match /home/jack.blanc/projects in opencode container
      - ${REPO_PATH_CYRUS:-~/projects/cyrus}:/home/jack.blanc/projects/cyrus
      - ${REPO_PATH_LINEAR_AGENT:-~/projects/linear-opencode-agent}:/home/jack.blanc/projects/linear-opencode-agent
      - ${REPO_PATH_MONEYDB:-~/projects/moneydb}:/home/jack.blanc/projects/moneydb
      - ${REPO_PATH_OPENCODE:-~/projects/opencode}:/home/jack.blanc/projects/opencode
      - ${REPO_PATH_RESERVATIONS:-~/projects/reservations}:/home/jack.blanc/projects/reservations
      - ${REPO_PATH_SST:-~/projects/sst}:/home/jack.blanc/projects/sst
      - ${REPO_PATH_WEATHER_BOT:-~/projects/weather-bot}:/home/jack.blanc/projects/weather-bot
      - ${REPO_PATH_WEBSITE:-~/projects/website}:/home/jack.blanc/projects/website
      # Worktrees directory for session isolation
      - worktrees:/home/jack.blanc/worktrees
    environment:
      - NODE_ENV=production
    depends_on:
      - opencode
    networks:
      - linear-agent

  # OpenCode server - AI coding agent
  # Port 4096 exposed to localhost only for UI access
  # NOT exposed via Tailscale Funnel - only accessible from your machine
  #
  # Uses /home/jack.blanc paths to match work Linux machine
  opencode:
    build:
      context: ./docker/opencode
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:4096:4096" # Localhost only - access OpenCode UI at http://localhost:4096
    volumes:
      # Mount worktrees - paths match between containers
      - worktrees:/home/jack.blanc/worktrees
      # Mount all repositories from ~/projects to /home/jack.blanc/projects
      - ${REPO_PATH_CYRUS:-~/projects/cyrus}:/home/jack.blanc/projects/cyrus
      - ${REPO_PATH_LINEAR_AGENT:-~/projects/linear-opencode-agent}:/home/jack.blanc/projects/linear-opencode-agent
      - ${REPO_PATH_MONEYDB:-~/projects/moneydb}:/home/jack.blanc/projects/moneydb
      - ${REPO_PATH_OPENCODE:-~/projects/opencode}:/home/jack.blanc/projects/opencode
      - ${REPO_PATH_RESERVATIONS:-~/projects/reservations}:/home/jack.blanc/projects/reservations
      - ${REPO_PATH_SST:-~/projects/sst}:/home/jack.blanc/projects/sst
      - ${REPO_PATH_WEATHER_BOT:-~/projects/weather-bot}:/home/jack.blanc/projects/weather-bot
      - ${REPO_PATH_WEBSITE:-~/projects/website}:/home/jack.blanc/projects/website
      # Persistent storage for OpenCode data (logs, sessions, etc.)
      - opencode-data:/home/jack.blanc/.local/share/opencode
    environment:
      # Optional: Anthropic API key (not needed if using Claude Max OAuth)
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      # GitHub token for git operations
      - GITHUB_TOKEN=${GITHUB_TOKEN}
    networks:
      - linear-agent

  # Cloudflare Tunnel - exposes linear-webhook publicly via Cloudflare Tunnel
  # Provides IP allowlisting via Cloudflare Access and DDoS protection
  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate --config /etc/cloudflared/config.yml run linear-webhook
    volumes:
      - ~/.cloudflared/config.yml:/etc/cloudflared/config.yml:ro
      - ~/.cloudflared/0d2577f8-b7f6-4839-a22a-990529fe9dac.json:/etc/cloudflared/credentials.json:ro
    networks:
      - linear-agent

volumes:
  linear-data:
  worktrees:
  opencode-data:

networks:
  linear-agent:
    driver: bridge
