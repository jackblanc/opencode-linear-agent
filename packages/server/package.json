{
  "name": "@opencode-linear-agent/server",
  "version": "0.1.3",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jackblanc/opencode-linear-agent.git"
  },
  "bin": {
    "opencode-linear-agent": "bin/opencode-linear-agent"
  },
  "files": [
    "bin",
    "postinstall.mjs"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "build": "rm -rf dist && bun run build:server",
    "build:server": "mkdir -p dist && bun build ./src/index.ts --outdir=dist --target=bun --packages=bundle",
    "build:bin": "mkdir -p dist && bun build ./src/index.ts --compile --outfile=dist/opencode-linear-agent",
    "generate:npm": "bun run ./script/generate-npm-packages.ts",
    "postinstall": "node ./postinstall.mjs || true",
    "prepublishOnly": "test -f bin/opencode-linear-agent && test -f postinstall.mjs"
  },
  "dependencies": {
    "@hono/zod-validator": "0.7.6",
    "hono": "4.12.12"
  },
  "devDependencies": {
    "@linear/sdk": "catalog:",
    "@opencode-ai/sdk": "catalog:",
    "@opencode-linear-agent/core": "workspace:*",
    "better-result": "catalog:",
    "zod": "catalog:"
  },
  "optionalDependencies": {
    "@opencode-linear-agent/server-darwin-arm64": "0.1.3",
    "@opencode-linear-agent/server-darwin-x64": "0.1.3",
    "@opencode-linear-agent/server-linux-arm64": "0.1.3",
    "@opencode-linear-agent/server-linux-x64": "0.1.3"
  }
}
